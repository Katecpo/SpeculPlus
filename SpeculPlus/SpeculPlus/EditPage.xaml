<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpeculPlus.EditPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Image Margin="10,10,0,0" Grid.Column="0" Source="{Binding Image}"/>

                <Label Text="Informations" Grid.Column="1" FontAttributes="Bold" />
                <Grid Margin="-10,20,0,0" Grid.Column="1">
                    <Grid Grid.Row="0">
                        <Label Text="Nom du produit" />
                        <Entry VerticalOptions="Start" x:Name="name" Text="{Binding Name}" TextColor="DarkGray"/>
                    </Grid>

                    <Grid Grid.Row="1">
                        <Label Text="Prix du produit" />
                        <Entry x:Name="price" Text="{Binding Price}" TextColor="DarkGray" />
                    </Grid>

                    <Grid Grid.Row="2">
                        <Label Text="Catégorie" TextColor="DarkGray"/>
                        <Picker x:Name="listCat" Title="Catégorie"/>
                    </Grid>
                </Grid>
            </Grid>
            

            

            <Grid VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand" HeightRequest="100" WidthRequest="200">
                </Grid>

            <Button Text="Terminé"
                    BackgroundColor="IndianRed"
                    TextColor="Black"
                    Clicked="AddProduct_Clicked"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>